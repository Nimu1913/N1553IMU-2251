FROM python:3.9-slim

WORKDIR /app

RUN pip install --no-cache-dir \
    fastapi \
    uvicorn \
    pillow \
    numpy \
    scipy \
    python-multipart

COPY src/api/car_simple.py /app/

CMD ["python", "-m", "uvicorn", "car_simple:app", "--host", "0.0.0.0", "--port", "8080"]
